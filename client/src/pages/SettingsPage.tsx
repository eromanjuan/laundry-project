import { PlaceholderPage } from './PlaceholderPage'

export function SettingsPage() {
  return (
    <PlaceholderPage
      title="Settings"
      subtitle="Configure business rules, operational preferences, and system defaults."
      description="This page will later contain branch settings, pricing defaults, user roles, and workspace configuration."
    />
  )
}
